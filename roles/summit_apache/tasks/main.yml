---
# tasks file for summit_apache
- name: install httpd
  ansible.builtin.yum:
    name: httpd

- name: copy default homepage
  ansible.builtin.copy:
    src: index.html
    dest: /var/www/html/index.html